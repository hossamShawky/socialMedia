pipeline {
    agent any

    stages {
        stage('List Directory') {
            steps {
                script {
                    // Replace '/path/to/your/directory' with the directory you want to list
                    sh 'ls'
                }
            }
        }
    }
}
